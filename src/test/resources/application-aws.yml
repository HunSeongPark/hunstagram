# AWS
cloud:
  aws:
    stack:
      auto: false
    region:
      static: ap-northeast-2
      auto: false
    s3:
      bucket: hunstagram-bucket

# Spring Multipart
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB